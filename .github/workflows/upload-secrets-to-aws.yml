name: Working Dynamic Upload to AWS SSM

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest

    env:
      # AWS credentials
      AWS_ACCESS_KEY_ID: ASIA2YTIIEU5IJJFY3A7
      AWS_SECRET_ACCESS_KEY: KkFLePEb+SB6tQZJonIazIqXzFhxyjuhD6ZbMHON
      AWS_SESSION_TOKEN: 
      AWS_REGION: us-west-1

    steps:
      - name: Test AWS Credentials
        run: |
          echo "🔐 Testing AWS credentials..."
          aws --version
          aws sts get-caller-identity

      - name: Upload GitHub Secrets to SSM (Dynamic)
        env:
          MY_SECRET1: ${{ secrets.MY_SECRET1 }}
          MY_SECRET2: ${{ secrets.MY_SECRET2 }}
        run: |
          echo "🔒 Uploading secrets to AWS SSM..."
          
          # Define secrets to upload
          declare -A secrets_map
          secrets_map["MY_SECRET1"]="$MY_SECRET1"
          secrets_map["MY_SECRET2"]="$MY_SECRET2"
          
          for secret_name in "${!secrets_map[@]}"; do
            value="${secrets_map[$secret_name]}"
            if [ -n "$value" ]; then
              echo "→ Uploading $secret_name"
              aws ssm put-parameter \
                --name "/test/$secret_name" \
                --value "$value" \
                --type "SecureString" \
                --region "$AWS_REGION" \
                --overwrite
              echo "✅ $secret_name uploaded successfully"
            else
              echo "⚠️ $secret_name is empty or not set"
            fi
          done

      - name: Upload GitHub Variables to SSM (Dynamic)
        env:
          MY_VAR1: ${{ vars.MY_VAR1 }}
          MY_VAR2: ${{ vars.MY_VAR2 }}
        run: |
          echo "📝 Uploading variables to AWS SSM..."
          
          # Define variables to upload
          declare -A vars_map
          vars_map["MY_VAR1"]="$MY_VAR1"
          vars_map["MY_VAR2"]="$MY_VAR2"
          
          for var_name in "${!vars_map[@]}"; do
            value="${vars_map[$var_name]}"
            if [ -n "$value" ]; then
              echo "→ Uploading $var_name (value: $value)"
              aws ssm put-parameter \
                --name "/test/$var_name" \
                --value "$value" \
                --type "String" \
                --region "$AWS_REGION" \
                --overwrite
              echo "✅ $var_name uploaded successfully"
            else
              echo "⚠️ $var_name is empty or not set"
            fi
          done

      - name: Retrieve Uploaded Parameters from SSM
        run: |
          echo "📥 All parameters under /test/:"
          aws ssm get-parameters-by-path \
            --path "/test/" \
            --with-decryption \
            --recursive \
            --query 'Parameters[*].[Name,Type,Value]' \
            --output table

          echo "✅ Upload and retrieval complete!"
