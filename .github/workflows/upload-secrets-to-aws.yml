name: Simple AWS Upload Test

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest

    env:
      # Hardcoded AWS credentials
      AWS_ACCESS_KEY_ID: ASIA2YTIIEU5IJJFY3A7
      AWS_SECRET_ACCESS_KEY: KkFLePEb+SB6tQZJonIazIqXzFhxyjuhD6ZbMHON
      AWS_SESSION_TOKEN: 
      AWS_REGION: us-west-2

    steps:
      - name: Test AWS Connection
        run: |
          echo "Testing AWS connection..."
          aws sts get-caller-identity

      - name: Upload GitHub Secrets to AWS SSM
        env:
          DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          API_KEY: ${{ secrets.API_KEY }}
          MONITORING_TOKEN: ${{ secrets.MONITORING_TOKEN }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
        run: |
          echo "Uploading secrets to AWS SSM..."
          
          # Upload DATABASE_PASSWORD
          if [ -n "$DATABASE_PASSWORD" ]; then
            aws ssm put-parameter \
              --name "/test/DATABASE_PASSWORD" \
              --value "$DATABASE_PASSWORD" \
              --type "SecureString" \
              --overwrite
            echo "✅ DATABASE_PASSWORD uploaded"
          fi

          # Upload DATABASE_URL
          if [ -n "$DATABASE_URL" ]; then
            aws ssm put-parameter \
              --name "/test/DATABASE_URL" \
              --value "$DATABASE_URL" \
              --type "SecureString" \
              --overwrite
            echo "✅ DATABASE_URL uploaded"
          fi

          # Upload API_KEY
          if [ -n "$API_KEY" ]; then
            aws ssm put-parameter \
              --name "/test/API_KEY" \
              --value "$API_KEY" \
              --type "SecureString" \
              --overwrite
            echo "✅ API_KEY uploaded"
          fi

          # Upload MONITORING_TOKEN
          if [ -n "$MONITORING_TOKEN" ]; then
            aws ssm put-parameter \
              --name "/test/MONITORING_TOKEN" \
              --value "$MONITORING_TOKEN" \
              --type "SecureString" \
              --overwrite
            echo "✅ MONITORING_TOKEN uploaded"
          fi

          # Upload REDIS_URL
          if [ -n "$REDIS_URL" ]; then
            aws ssm put-parameter \
              --name "/test/REDIS_URL" \
              --value "$REDIS_URL" \
              --type "SecureString" \
              --overwrite
            echo "✅ REDIS_URL uploaded"
          fi

      - name: Upload GitHub Variables to AWS SSM
        env:
          ENVIRONMENT: ${{ vars.ENVIRONMENT }}
          APP_VERSION: ${{ vars.APP_VERSION }}
          CLUSTER_NAME: ${{ vars.CLUSTER_NAME }}
          ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY }}
        run: |
          echo "Uploading variables to AWS SSM..."
          
          # Upload ENVIRONMENT
          if [ -n "$ENVIRONMENT" ]; then
            aws ssm put-parameter \
              --name "/test/ENVIRONMENT" \
              --value "$ENVIRONMENT" \
              --type "String" \
              --overwrite
            echo "✅ ENVIRONMENT uploaded: $ENVIRONMENT"
          fi

          # Upload APP_VERSION
          if [ -n "$APP_VERSION" ]; then
            aws ssm put-parameter \
              --name "/test/APP_VERSION" \
              --value "$APP_VERSION" \
              --type "String" \
              --overwrite
            echo "✅ APP_VERSION uploaded: $APP_VERSION"
          fi

          # Upload CLUSTER_NAME
          if [ -n "$CLUSTER_NAME" ]; then
            aws ssm put-parameter \
              --name "/test/CLUSTER_NAME" \
              --value "$CLUSTER_NAME" \
              --type "String" \
              --overwrite
            echo "✅ CLUSTER_NAME uploaded: $CLUSTER_NAME"
          fi

          # Upload ECR_REPOSITORY
          if [ -n "$ECR_REPOSITORY" ]; then
            aws ssm put-parameter \
              --name "/test/ECR_REPOSITORY" \
              --value "$ECR_REPOSITORY" \
              --type "String" \
              --overwrite
            echo "✅ ECR_REPOSITORY uploaded: $ECR_REPOSITORY"
          fi

      - name: Retrieve and Display from AWS SSM
        run: |
          echo "=== Retrieving from AWS SSM ==="
          
          echo "📋 All parameters under /test/:"
          aws ssm get-parameters-by-path \
            --path "/test/" \
            --recursive \
            --query 'Parameters[*].[Name,Type]' \
            --output table

          echo "🔒 Secrets (values hidden for security):"
          aws ssm get-parameters-by-path \
            --path "/test/" \
            --parameter-filters "Key=Type,Values=SecureString" \
            --query 'Parameters[*].Name' \
            --output table

          echo "📝 Variables (values visible):"
          aws ssm get-parameters-by-path \
            --path "/test/" \
            --parameter-filters "Key=Type,Values=String" \
            --query 'Parameters[*].[Name,Value]' \
            --output table

          echo "✅ Upload and retrieval complete!"
