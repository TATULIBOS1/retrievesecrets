name: Hardcoded Upload to AWS SSM

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest

    env:
      # 🔐 AWS credentials for testing (not recommended for production)
      AWS_ACCESS_KEY_ID: ASIA2YTIIEU5IJJFY3A7
      AWS_SECRET_ACCESS_KEY: KkFLePEb+SB6tQZJonIazIqXzFhxyjuhD6ZbMHON
      AWS_SESSION_TOKEN: 
      AWS_REGION: us-west-2

      # 🧩 Dynamic lists
      SECRET_NAMES: DATABASE_PASSWORD,DATABASE_URL,API_KEY,MONITORING_TOKEN,REDIS_URL
      VAR_NAMES: ENVIRONMENT,APP_VERSION,CLUSTER_NAME,ECR_REPOSITORY

      # 🔐 Sample secret values (in real use, fetch from GitHub Secrets)
      DATABASE_PASSWORD: test_db_pass
      DATABASE_URL: jdbc://localhost:3306/test
      API_KEY: abc123key
      MONITORING_TOKEN: zabbix-token
      REDIS_URL: redis://localhost:6379

      # 🔧 Sample variable values
      ENVIRONMENT: dev
      APP_VERSION: 1.0.2
      CLUSTER_NAME: test-cluster
      ECR_REPOSITORY: sample-repo

    steps:
      - name: Check AWS Connection
        run: |
          echo "Testing AWS connection..."
          aws sts get-caller-identity

      - name: Upload Secrets to AWS SSM
        run: |
          echo "🔐 Uploading secrets to AWS SSM..."
          IFS=',' read -ra NAMES <<< "$SECRET_NAMES"
          for name in "${NAMES[@]}"; do
            value="${!name}"
            echo "→ Uploading $name"
            aws ssm put-parameter \
              --name "/test/$name" \
              --value "$value" \
              --type "SecureString" \
              --region "$AWS_REGION" \
              --overwrite
          done

      - name: Upload Variables to AWS SSM
        run: |
          echo "⚙️ Uploading variables to AWS SSM..."
          IFS=',' read -ra NAMES <<< "$VAR_NAMES"
          for name in "${NAMES[@]}"; do
            value="${!name}"
            echo "→ Uploading $name"
            aws ssm put-parameter \
              --name "/test/$name" \
              --value "$value" \
              --type "String" \
              --region "$AWS_REGION" \
              --overwrite
          done

      - name: Retrieve and Display from AWS SSM
        run: |
          echo "📥 Retrieving parameters from SSM..."

          echo "🔐 SecureString parameters:"
          aws ssm get-parameters-by-path \
            --path "/test/" \
            --recursive \
            --parameter-filters "Key=Type,Values=SecureString" \
            --query 'Parameters[*].Name' \
            --output table

          echo "⚙️ String parameters with values:"
          aws ssm get-parameters-by-path \
            --path "/test/" \
            --recursive \
            --parameter-filters "Key=Type,Values=String" \
            --query 'Parameters[*].[Name,Value]' \
            --output table

          echo "✅ Upload and retrieval complete."
